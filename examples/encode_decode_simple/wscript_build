#! /usr/bin/env python
# encoding: utf-8

bld.program(features = 'c',
            source   = 'encode_decode_simple.c',
            target   = 'encode_decode_simple',
            #lib      = ['stdc++'],
            use      = ['ckodo'])

